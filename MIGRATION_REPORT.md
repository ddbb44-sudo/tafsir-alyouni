# MIGRATION_REPORT.md — تقرير الترحيل

## الحالة العامة

| # | الصفحة | المستودع القديم | الحالة |
|---|---|---|---|
| 1 | تفسير سورة الفاتحة | `AL-fataha` | ✅ **مُرحَّلة ومعتمدة** (بناء ناجح + مراجعة بصرية) |
| 2 | تفسير آية الكرسي | `Al-Kursi` | ✅ **مُرحَّلة — بانتظار البناء والمراجعة** |
| 3 | تفسير سورة الإخلاص | `Al-Ikhlas` | ✅ **مُرحَّلة — بانتظار البناء والمراجعة** |
| 4 | تفسير آخر آيتين من البقرة | `alBaqrah_last` | ✅ **مُرحَّلة — بانتظار البناء والمراجعة** |
| 5 | تفسير سورة الفلق | `Al-Falaq` | ✅ **مُرحَّلة — بانتظار البناء والمراجعة** |
| 6 | تفسير سورة الناس | `Al-Nas` | ✅ **مُرحَّلة — بانتظار البناء والمراجعة** |

**لم يُعدَّل أي مستودع قديم ولم يُحذف أي ملف منه.** المصادر في `_sources/` للقراءة فقط.

---

## ١ — سورة الفاتحة

**المصدر:** `_sources/AL-fataha/index.html` (106,806 بايت)
**الوجهة:** `/tafsir/al-fatiha`
**الرابط القديم:** https://ddbb44-sudo.github.io/AL-fataha/ — **ما يزال يعمل، لم يُمس**

### الملفات المنشأة

| الملف | الحجم | المحتوى |
|---|---|---|
| `meta.json` | ‎~1 ك‎ | البيانات والمراسي والميزات |
| `summary.mdx` | ‎~15 ك‎ | مختصر التفسير |
| `topics.mdx` | ‎~5 ك‎ | التصنيف الموضوعي (٥ بطاقات) |
| `full-text.mdx` | ‎~54 ك‎ | النص كاملًا |

### التحقق من سلامة النص ✅

```
✓ summary   : مطابق تمامًا (6,103 حرفًا)
✓ topics    : مطابق تمامًا (2,253 حرفًا)
✓ full-text : مطابق تمامًا (27,752 حرفًا)

الإجمالي المقارَن: 36,108 حرفًا
✓ النص العربي محفوظ بالكامل — لا فروق.
```

المقارنة آلية حرفًا بحرف بين نص الصفحة الأصلية ونص الملفات الناتجة، بعد توحيد المسافات فقط.
لإعادة التحقق في أي وقت:

```bash
npm run verify:content -- ../_sources/AL-fataha/index.html al-fatiha
```

### المراسي

| المعتمد | الأسماء البديلة (تعمل تلقائيًا) |
|---|---|
| `part-1` `part-2` `part-3` | `part1` `part2` `part3` |
| `cat-meanings` | `sec-meanings` · `meanings` |
| `cat-virtues` | `sec-virtues` · `virtues` |
| `cat-benefits` | `sec-benefits` · `benefits` |
| `cat-theology` | `sec-theology` · `theology` |
| `cat-fiqh` | `sec-fiqh` · `fiqh` |

الفاتحة كانت تستعمل `part-1/2/3` أصلًا، فلم يتغير شيء لها. الأسماء البديلة أضيفت استعدادًا
لتوحيد بقية الصفحات التي تستعمل `part1` بلا شرطة.

### الميزات المحفوظة

- [x] الاتجاه من اليمين إلى اليسار
- [x] الشريط العلوي الثابت الشفاف
- [x] شريط أقسام الصفحة مع تظليل القسم الحالي (`IntersectionObserver` بالهوامش نفسها)
- [x] التمرير الناعم مع إزاحة المرسى
- [x] أزرار حجم الخط ‎0.8–1.4‎ مع الحفظ
- [x] الوضع الليلي مع الحفظ
- [x] فتح/إغلاق كل التفاصيل اللغوية مع الحفظ
- [x] زر نسخ النص كاملًا
- [x] قائمة التنقل بين السور
- [x] الاستجابة للجوال
- [x] خط Amiri للقرآن و IBM Plex Sans Arabic للواجهة

### الفروق المقصودة عن الصفحة الأصلية

| # | الفرق | السبب |
|---|---|---|
| 1 | **زر الوضع الليلي صار ظاهرًا ويعمل** | كان `menu.js` يخفيه قسرًا ويفرض تفضيل النظام. المالك طلب صراحة إرجاعه مع النظام كقيمة ابتدائية (الإجابة A3). |
| 2 | **حُذف الاعتماد على `AL-fataha/menu.js`** | كانت الصفحات الست تحمّله من مستودع واحد. صار مكوّنات داخل المشروع. المستودع القديم لم يُمس، فالصفحات القديمة ما تزال تعمل. |
| 3 | **Tailwind CDN → CSS مبني** | تحميل مصرّف Tailwind وقت التشغيل يسبب بطئًا ووميضًا وغير موصى به للإنتاج. النتيجة البصرية نفسها. |
| 4 | **توحيد تباعد القوائم** | الأصل خلط `space-y-1/2/3` بلا نمط. وُحّد إلى قاعدة واحدة. فرق بصري طفيف جدًا. |
| 5 | **`<span class="font-semibold">` → `<strong>`** | العنصر كان تنسيقًا محضًا؛ صار دلاليًا. النص والمظهر نفسهما. |
| 6 | **أُضيف تذييل** | لم يكن في الصفحات الأصلية تذييل إطلاقًا. |
| 7 | **أُضيف سيو** | لم يكن هناك وصف ولا canonical ولا Open Graph ولا خريطة موقع. إضافة صافية. |
| 8 | **أُضيف رابط تخطّي وتحسينات وصول** | `:focus-visible`، `aria-*`، ودعم `prefers-reduced-motion`. |

**لا شيء من هذه الفروق يمسّ النص العربي.** التحقق الآلي أعلاه يثبت ذلك.

### مسائل ظهرت أثناء الترحيل وحُلّت

| المسألة | الحل |
|---|---|
| المحوّل كان يُسقط محتوى `#full-text-content` بصمت | جعل الحاوية شفافة؛ **الفحص الصارم هو ما كشف الإسقاط** |
| المحوّل أضاف مسافة قبل العناصر السطرية (`﴾ ،` بدل `﴾،`) | العناصر السطرية تُكتب في مكانها بلا سطر جديد |
| تسميات شريط التنقل خُلطت بعناوين الأقسام | فصل الحقلين: `parts` للعناوين و `anchors` للتنقل. الفاتحة: القسم «مختصر التفسير» والزر «البداية» |
| `class` داخل MDX سلوكه غير مضمون | استُبدل بـ `data-muted="true"` — لا يوجد أي `class` في ملفات المحتوى |
| `<Button>` كان يُسقط `data-target` | تمرير بقية الخصائص عبر `...rest` |
| لوحة القائمة استعملت خصائص منطقية مع إزاحة فيزيائية | خصائص فيزيائية (`right`) لمطابقة الأصل |

### المتبقي على هذه الصفحة

- [ ] `npm install && npm run build` على جهاز المالك (البيئة هنا بلا اتصال بالشبكة)
- [ ] مراجعة بصرية ومقارنة بالصفحة القديمة
- [ ] موافقة المالك

---

## ٢ — آية الكرسي

**المصدر:** `_sources/Al-Kursi/index.html` (116,614 بايت)
**الوجهة:** `/tafsir/ayat-al-kursi`
**الرابط القديم:** https://ddbb44-sudo.github.io/Al-Kursi/ — **ما يزال يعمل، لم يُمس**

### التحقق من سلامة النص ✅

```
✓ summary   : مطابق تمامًا (5,977 حرفًا)
✓ topics    : مطابق تمامًا (1,592 حرفًا)
✓ full-text : مطابق تمامًا (27,999 حرفًا)

الإجمالي المقارَن: 35,568 حرفًا
✓ النص العربي محفوظ بالكامل — لا فروق.
```

وأُعيد التحقق من الفاتحة بعد التعديلات: **ما تزال مطابقة تمامًا (36,108 حرفًا)** — لا ارتداد.

### المراسي

الصفحة القديمة استعملت `part1/part2/part3` **بلا شرطة**. المعتمد الآن `part-1/2/3`
والقديم يعمل تلقائيًا كاسم بديل.

| المعتمد | الأسماء البديلة |
|---|---|
| `part-1` `part-2` `part-3` | `part1` `part2` `part3` |
| `cat-meanings` `cat-virtues` `cat-benefits` `cat-aqeedah` | `sec-*` و الاسم المجرّد |

✅ الرابط القديم `Al-Kursi/#part3` سيعمل بعد إعادة التوجيه.

### الميزات

| الميزة | الحالة |
|---|---|
| التفاصيل اللغوية (٣ عناصر) | ✅ محفوظة (`linguisticDetails: true`) |
| زر نسخ النص كاملًا | ❌ **لم يكن موجودًا في هذه الصفحة أصلًا** — لذلك `copyFullText: false` |
| بقية الأدوات | ✅ من النظام المشترك |

### ⚠️ فروق بصرية تحتاج انتباهك عند المراجعة

هذه الصفحة كانت **مصمَّمة تصميمًا مختلفًا عن الفاتحة**، ووُحِّدت على نظام التصميم المشترك
كما يقتضي الهدف من إعادة الهيكلة. **لم يتغيّر أي نص**، لكن المظهر تغيّر في النقاط التالية:

| # | الأصل | الآن | لماذا |
|---|---|---|---|
| 1 | عناوين بطاقات التصنيف بألوان مختلفة (بنفسجي، نيلي، زمردي، أزرق) | لون واحد موحّد (`--quran-red`) | «نظام ألوان واحد مركزي» — الألوان كانت زخرفية لا دلالية |
| 2 | عناوين ملوّنة (تركوازي/أزرق) داخل بطاقتي الفضائل والمناسبات | نمط العنوان الفرعي الموحّد | نفس السبب |
| 3 | صناديق تنبيه زرقاء | `<Note>` بالخط الأزرق الجانبي | توحيد مع نمط الفاتحة |
| 4 | صناديق شعر رمادية موسّطة مائلة | `<Note variant="quote">` | **حوفظ على تمييزها** بنمط خاص |
| 5 | تمييز القرآن عن الأدلة بلونين (`quran` / `evidence`) | **حوفظ عليه**: أحمر للقرآن، أزرق للأدلة | تمييز دلالي حقيقي يقابل `--quran-red` / `--quran-blue` |
| 6 | شريط علوي ملوّن يحمل الآية داخل البطاقة | مكوّن `<CardBanner>` جديد | بنية حقيقية، حوفظ عليها |

**إن أردت إبقاء ألوان البطاقات المختلفة لهذه الصفحة أخبرني** — يمكن إضافتها كخيار في
`meta.json` دون كسر النظام الموحّد. لكن توصيتي إبقاء التوحيد.

### تحسينات على أدوات الترحيل (تفيد الصفحات الأربع الباقية)

| التحسين | الأثر |
|---|---|
| طبقة **قواعد بالأنماط** (`RULES`) قبل الجدول الحرفي | المطابقة صارت بالمعنى لا بنص أصناف Tailwind، فتتوسّع للصفحات المختلفة التصميم |
| الاستيرادات تُشتق تلقائيًا من المكوّنات المستعملة | لا قوائم يدوية |
| بطاقات التصنيف تُلتقط بالمرسى لا بالصنف | يعمل مع اختلاف تصميم كل صفحة |
| `scripts/lint-mdx.mjs` صار يعمل تلقائيًا قبل البناء | كشف خطأين في `<LinguisticDetails>` قبل البناء |
| إصلاح نفس خطأ الفقرة في فرع `<details>` | كان سيفشل البناء |

### المتبقي

- [ ] `npm run build` على جهازك
- [ ] مراجعة بصرية، مع الانتباه لجدول الفروق البصرية أعلاه
- [ ] موافقتك

---

## ٣ — الدفعة الأخيرة: الإخلاص، البقرة، الفلق، الناس

رُحِّلت الصفحات الأربع دفعة واحدة، ثم أُعيد التحقق من الصفحتين السابقتين.

### التحقق من سلامة النص — الصفحات الست ✅

| الصفحة | مختصر التفسير | التصنيف الموضوعي | النص كاملًا | الإجمالي |
|---|---:|---:|---:|---:|
| الفاتحة | 6,103 | 2,253 | 27,752 | **36,108** |
| آية الكرسي | 5,977 | 1,592 | 27,999 | **35,568** |
| الإخلاص | 4,814 | 1,628 | 15,185 | **21,627** |
| آخر آيتين البقرة | 7,163 | 2,181 | 39,893 | **49,237** |
| الفلق | 4,949 | 1,592 | 12,068 | **18,609** |
| الناس | 3,846 | 1,400 | 10,633 | **15,879** |
| | | | **المجموع** | **177,028 حرفًا** |

**كل الأقسام الثمانية عشر مطابقة تمامًا. لا فرق حرف واحد.**
ولا ارتداد في الفاتحة وآية الكرسي بعد توسعة المحوّل.

```
node scripts/lint-mdx.mjs   →  فُحص 18 ملف MDX · ✓ بنية MDX سليمة.
```

### ⚠️ ثغرة تحقّق اكتُشفت وأُغلقت

في أول تشغيل ظهر لسورة الناس: `— topics: لا يوجد في المصدر`.

السبب: قسم التصنيف في سورة الناس **بلا `id` في الأصل** (الخلل المرصود في مرحلة الفحص).
المستخرِج كان يجده بالبديل، لكن **المدقّق لم يكن يعرف هذا البديل**، فمرّ القسم بلا تدقيق.

النتيجة: **1,400 حرفًا كانت ستُنشر دون التحقق منها.** لم تكن ستُفقد، لكنها كانت ستبقى
خارج الضمان. أُصلح المدقّق ليستعمل منطق المستخرِج نفسه، وتأكد التطابق.

**الدرس:** أي منطق للعثور على المحتوى يجب أن يكون مشتركًا بين المستخرِج والمدقّق.

### الفروق البصرية المقصودة (مطبَّقة على الصفحات الأربع)

وفق قرارك: توحيد كامل، وإبقاء التمييز الدلالي فقط.

| # | الأصل | الآن |
|---|---|---|
| 1 | ألوان مميزة لكل صفحة (نيلي في الفلق، أزرق في الناس، ألوان متعددة في البقرة) | نظام الألوان الموحّد |
| 2 | أشرطة وخطوط ونقاط زخرفية تحت العناوين | حُذفت (لا نص فيها) |
| 3 | أشكال خلفية مزخرفة خلف البطاقات (البقرة) | حُذفت |
| 4 | صناديق ملوّنة متعددة (نيلي، أردوازي، رمادي) | `<Note>` موحّد |
| 5 | لصيقات دائرية (البقرة) | `<Badge>` موحّد |
| 6 | حشوات وهوامش مختلفة لكل صفحة | مقاسات `tokens.css` |

### التمييزات الدلالية المحفوظة

| التمييز | كيف عُرِف في الأصل | المقابل الموحّد |
|---|---|---|
| نص قرآني | `font-quran` · `font-amiri` · `text-quran-main` · `text-quranic` · `quran-main` · `quran-passage` | `<Ayah>` / `<QuranBlock>` — أحمر |
| دليل أو حديث | `quran-evidence` · `text-quran-evidence` · `evidence` | `<Ayah tone="blue">` — أزرق |
| تنبيه | صناديق ملوّنة | `<Note>` |
| شعر واقتباس | صندوق موسّط مائل | `<Note variant="quote">` |
| تفاصيل لغوية | `<details>` / `linguistic-details` | `<LinguisticDetails>` |
| لصيقة | `rounded-full inline-block` | `<Badge>` |
| نص ثانوي | `text-sm text-gray-500` | `<span data-muted>` |

### إصلاحات وميزات

| البند | التفصيل |
|---|---|
| **`part-2` بلا id في سورة الناس** | أُصلح — القسم صار له مرسى صحيح. **لا تغيير في النص.** |
| **مراسي البقرة المجرّدة** | `#meanings` → `cat-meanings` مع إبقاء `meanings` اسمًا بديلًا |
| **مراسي الفلق `sec-*`** | `#sec-meanings` → `cat-meanings` مع إبقاء `sec-meanings` |
| **زر النسخ** | مفعّل الآن في **الصفحات الست** حسب طلبك — بما فيها آية الكرسي والبقرة والفلق التي لم يكن فيها زر أصلًا |
| **التفاصيل اللغوية** | مفعّلة حيث توجد `<details>` (كل الصفحات) |

### توحيد المراسي

| الصفحة | الأصل | المعتمد | الأسماء البديلة العاملة |
|---|---|---|---|
| الفاتحة · الإخلاص · الفلق · الناس | `part-1/2/3` | `part-1/2/3` | `part1` `part2` `part3` |
| الكرسي · البقرة | `part1/2/3` | `part-1/2/3` | `part1` `part2` `part3` |
| الفلق | `sec-*` | `cat-*` | `sec-*` و الاسم المجرّد |
| البقرة | مجرّد (`#meanings`) | `cat-*` | الاسم المجرّد و `sec-*` |

✅ كل رابط منشور سابقًا سيظل يعمل بعد إعادة التوجيه.

### نقاط تستحق مراجعتك البصرية

1. **آخر آيتين البقرة** — الأكبر (49,237 حرفًا) والأكثر اختلافًا في التصميم. راجعها أولًا.
2. **سورة الناس** — تأكد أن قسم «التصنيف الموضوعي» صار له مرسى يعمل في شريط التنقل.
3. **سورة الفلق** — كانت الأكثر تميزًا (لون نيلي، أزرار بأسماء مختلفة). تأكد من التفاصيل اللغوية.
4. **زر النسخ** — جديد في ثلاث صفحات؛ جرّبه في كل صفحة.
5. **`<Badge>` و `<Note variant="quote">`** — مكوّنان جديدان، ظهورهما الأول في هذه الدفعة.

---

## ملاحظات لبقية الصفحات

مما رُصد في مرحلة الفحص، ويجب الانتباه له عند ترحيل كل صفحة:

| الصفحة | ما يحتاج انتباهًا |
|---|---|
| `Al-Kursi` | مراسٍ بلا شرطة (`part1`) + `cat-aqeedah` · لا يوجد README |
| `Al-Ikhlas` | مراسٍ بشرطة · `btn-copy` باسم مختلف · `cat-revelation` و `cat-theology` |
| `alBaqrah_last` | **أصعبها**: مراسٍ بلا شرطة وتصنيفات **بلا بادئة** (`#meanings`) · أزرار بأسماء مختلفة كليًا · أكبر ملف (133 ك) |
| `Al-Falaq` | **الأكثر اختلافًا**: بادئة `sec-` · أزرار `btn-text-*` و `btn-theme-toggle` · **ميزة التفاصيل اللغوية `btn-toggle-linguistics` تخصه وحده** |
| `Al-Nas` | **قسم `part-2` بلا `id` — خلل حقيقي**، سيُصلح بإضافة المرسى (لا تغيير في النص) |

كل صفحة ستمر بالدورة نفسها: تحويل → تحقق آلي → مراجعة → موافقة.

---

## خطة الروابط القديمة (بعد الموافقة على كل الصفحات)

لن يُحذف أي مستودع. بعد اكتمال الترحيل وموافقة المالك، يتحوّل كل مستودع قديم إلى صفحة
إعادة توجيه تحافظ على جزء المرسى:

```html
<script>location.replace("https://ddbb44-sudo.github.io/tafsir-alyouni/tafsir/al-fatiha"+location.hash)</script>
```

⚠️ **ترتيب إلزامي:** `AL-fataha` **آخر** ما يُحوَّل، لأن الصفحات الخمس الأخرى ما تزال تحمّل
`/AL-fataha/menu.js`. تحويله قبلها يكسر قائمة التنقل فيها.
