# CONTENT_GUIDE.md — دليل المحتوى

## القاعدة الأولى والأخيرة

النص منقول حرفيًا من دروس الشيخ سليمان العيوني. **الحفاظ عليه مقدَّم على أي اعتبار تقني أو جمالي.**

لا تُعِد الصياغة. لا تلخّص. لا تصحّح. لا تُعِد الترتيب. إن رأيت ما تظنه خطأ فأبلغ المالك ولا تُصلحه.

---

## ١. تقسيم المحتوى

كل صفحة ثلاثة ملفات، والسبب عملي: حتى يفتح المساعد الذكي ملفًا صغيرًا واحدًا بدل صفحة ضخمة.

| الملف | المحتوى | المرسى |
|---|---|---|
| `summary.mdx` | مختصر التفسير — الآيات مع شرحها مقطعًا مقطعًا | `part-1` |
| `topics.mdx` | التصنيف الموضوعي — بطاقات (المعاني، الفضائل…) | `part-2` |
| `full-text.mdx` | النص كاملًا — تفريغ الدرس | `part-3` |

### إن كان قسم غير موجود في صفحة ما

احذف اسمه من `sections` في `meta.json` **ولا تنشئ ملفًا فارغًا**:

```json
{ "sections": ["summary", "full-text"] }
```

يتخطّى `[slug].astro` القسم تلقائيًا، ولا يظهر فاصل زائد. واحذف كذلك زر التنقل المقابل من `anchors`.

---

## ٢. المكوّنات المتاحة داخل ملفات MDX

استورد ما تحتاجه فقط في أعلى الملف.

| المكوّن | الاستعمال |
|---|---|
| `<TafsirCard>` | بطاقة. `variant="quran"` للآية الرئيسية، `variant="compact"` لبطاقة أصغر |
| `<SectionTitle level={3\|4} first>` | عنوان فرعي بالشريط الأحمر. `first` يلغي المسافة العلوية |
| `<TopicTitle>` | عنوان أحمر بخط سفلي |
| `<TopicCard id="cat-x" title="…">` | بطاقة تصنيف موضوعي بمرساها |
| `<QuranBlock>` | آية معروضة ككتلة. `variant="support"` أزرق، `variant="opening"` للبسملة |
| `<Ayah>` | اقتباس قرآني داخل السطر. `tone="blue"` للمراجع، `size="inherit"` بحجم النص |
| `<Note>` | فقرة منبّهة بخط أزرق جانبي. `emphasis="lg"` أكبر ومائل |
| `<LinguisticDetails summary="…">` | تفاصيل لغوية مطوية — يتحكم بها زر الشريط العلوي |

عناصر HTML العادية `<p> <ul> <ol> <li> <strong> <br />` تُستعمل مباشرة.

---

## ٣. قواعد كتابة يجب التزامها

**١. لا تكتب `class` داخل ملفات MDX.** للقائمة الثانوية استعمل:

```mdx
<ul data-muted="true"> … </ul>
```

**٢. أغلق كل وسم.** MDX لا يتسامح: `<br>` خطأ، `<br />` صحيح.

**٣. تجنّب `{` و `}` في النص.** MDX يفسّرهما كتعبير برمجي. إن اضطررت اكتب `&#123;` و `&#125;`.

**٤. لا تضع سطرًا فارغًا داخل عنصر سطري** — قد يفسّره MDX كفقرة جديدة.

**٥. لا تضف مسافة قبل عنصر سطري ولم تكن في الأصل.** هذه المسافة تظهر للقارئ:

```mdx
قوله تعالى: <Ayah>﴿الحمد لله﴾</Ayah>، برفع الحمد.   ✅
قوله تعالى: <Ayah>﴿الحمد لله﴾</Ayah> ، برفع الحمد.  ❌ فراغ قبل الفاصلة
```

---

## ٤. الحفاظ على القرآن والحديث

- انسخ ولا تكتب من الذاكرة — **أبدًا**.
- أبقِ ﴿ ﴾ كما هي، ولا تستبدلها بأقواس أخرى.
- أبقِ التشكيل كاملًا كما ورد.
- لا تُوحّد الرسم الإملائي ولا "تُصلح" الهمزات.
- لا تُطبّق أي تطبيع Unicode (normalization) على الملفات.

---

## ٥. المراسي

- الصيغة المعتمدة: `part-1/2/3` و `cat-*`.
- المراسي القديمة تُولَّد تلقائيًا (`part1`، `sec-x`، `x`) — **لا تحذفها**.
- عند إضافة تصنيف جديد استعمل `cat-` دائمًا، وسجّله في `anchors` ليظهر في شريط التنقل.

---

## ٦. التحقق بعد أي تعديل

```bash
npm run verify:content -- ../_sources/AL-fataha/index.html al-fatiha
npm run build
```

الأداة تقارن نص الصفحة الأصلية بالناتج حرفًا بحرف. أي فرق يعني أن شيئًا ضاع أو تغيّر.

⚠️ عند تعديل مقصود بطلب المالك سيفشل التحقق بطبيعة الحال — وهذا متوقع. وثّق التعديل في
`MIGRATION_REPORT.md` حتى لا يُظن لاحقًا أنه خطأ ترحيل.

---

## ٧. أخطاء شائعة

| الخطأ | الصواب |
|---|---|
| تعديل صياغة لتحسين الأسلوب | انسخ كما هو |
| حذف تكرار يبدو زائدًا | التكرار في الدرس مقصود — أبقِه |
| توحيد عناوين متشابهة بين الصفحات | كل صفحة لها تصنيفها الخاص |
| حذف مرسًى قديم | قد يكون منشورًا — أبقِه |
| كتابة `class="muted"` | `data-muted="true"` |
| إصلاح أداة التحقق لتمرير الاختبار | أصلح السبب الحقيقي |
