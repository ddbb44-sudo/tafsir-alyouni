# AGENTS.md — تعليمات للمساعد الذكي (Claude / Codex)

هذا الملف موجَّه للمساعد الآلي قبل أي تعديل على هذا المشروع. اقرأه أولًا.

---

## ١. القاعدة الأهم: لا تُعدّل النص الديني

النص العربي في `src/content/tafsir/**/*.mdx` منقول حرفيًا من دروس الشيخ سليمان العيوني.

**ممنوع منعًا باتًا، إلا بطلب صريح ومحدَّد من المالك:**

- إعادة صياغة أي جملة، أو "تحسين" الأسلوب.
- تلخيص أو اختصار أي فقرة.
- تصحيح ما تظنه خطأ إملائيًا أو نحويًا أو علميًا.
- تعديل آية أو حديث أو بيت شعر أو مرجع أو عنوان.
- تغيير التشكيل أو علامات الترقيم أو رموز الآيات ﴿ ﴾.
- إعادة ترتيب الفقرات.

إن رأيتَ ما تظنه خطأ: **أبلغ المالك ولا تُصلحه من تلقاء نفسك.**

بعد أي تعديل يمسّ ملفات المحتوى شغّل:

```bash
npm run verify:content -- ../_sources/AL-fataha/index.html al-fatiha
```

ويجب أن تكون النتيجة: `✓ النص العربي محفوظ بالكامل — لا فروق.`

---

## ٢. أين يوجد كل شيء؟

| ماذا تريد أن تغيّر؟ | الملف الذي تفتحه | لا تفتح غيره |
|---|---|---|
| الألوان (نهاري/ليلي) | `src/styles/tokens.css` | — |
| أحجام الخط وارتفاع السطر | `src/styles/tokens.css` | — |
| شكل النصوص والعناوين والقوائم | `src/styles/typography.css` | — |
| الشريط العلوي وأدوات القارئ والبطاقات | `src/styles/reader.css` | — |
| الترويسة (الشريط العلوي) | `src/components/layout/Header.astro` | — |
| التذييل | `src/components/layout/Footer.astro` | — |
| قائمة التنقل بين السور | `src/components/layout/MenuDrawer.astro` | — |
| أزرار حجم الخط | `src/components/reader/FontSizeControl.astro` | — |
| ألوان الوضع الليلي | `src/styles/tokens.css` — كتلة `@media (prefers-color-scheme: dark)` | — |
| شريط أقسام الصفحة | `src/components/reader/InternalPageNav.astro` | — |
| ترتيب الصفحة ككل | `src/layouts/TafsirLayout.astro` | — |
| بيانات صفحة واحدة (عنوان، مراسي، SEO) | `src/content/tafsir/<slug>/meta.json` | — |
| نص صفحة واحدة | `src/content/tafsir/<slug>/{summary,topics,full-text}.mdx` | — |
| اسم الموقع ووصفه | `src/data/site.ts` | — |
| مسار النشر (base) | `astro.config.mjs` | — |

**القاعدة:** تغيير الترويسة أو التذييل أو الألوان = **ملف واحد فقط**. إن وجدت نفسك تعدّل الشيء نفسه في أكثر من ملف، فأنت تفعل شيئًا خاطئًا — توقّف واسأل.

---

## ٣. كيف تعدّل صفحة واحدة دون لمس غيرها؟

المحتوى مقسوم عمدًا إلى ثلاثة ملفات حتى لا تفتح ملفًا ضخمًا بلا داعٍ:

| إن طُلب منك… | افتح هذا فقط |
|---|---|
| تعديل مختصر التفسير | `summary.mdx` |
| تعديل التصنيف الموضوعي | `topics.mdx` |
| تعديل النص الكامل | `full-text.mdx` |
| تعديل العنوان أو الوصف أو المراسي | `meta.json` |

لا تفتح الثلاثة إن كان المطلوب واحدًا.

---

## ٤. ملفات لا تُعدَّل إلا بحذر شديد

| الملف | لماذا |
|---|---|
| `src/content/tafsir/**/*.mdx` | نص ديني منقول حرفيًا — اقرأ §١ |
| `scripts/extract-page.mjs` | أداة ترحيل الصفحات القديمة؛ تعديلها الخاطئ يفسد ترحيل بقية السور |
| `scripts/verify-content.mjs` | أداة التحقق؛ لا تُضعِفها لتمرير اختبار فاشل — أصلح السبب |
| `src/utils/anchors.ts` | يحمي الروابط المنشورة القديمة من الكسر |
| `astro.config.mjs` | تغيير `base` يكسر كل الروابط والأصول |
| `.github/workflows/deploy.yml` | النشر التلقائي |

**قاعدة صارمة:** إن فشل `verify:content` فالخلل في التحويل، لا في أداة التحقق. لا تعدّل أداة التحقق لتُسكِت الخطأ.

---

## ٥. الروابط الداخلية

لا تكتب مسارًا داخليًا يدويًا أبدًا. الموقع منشور تحت `/tafsir-alyouni/`.

```astro
---
import { withBase } from '../utils/paths';
---
<a href={withBase('/tafsir/al-fatiha')}>الفاتحة</a>   <!-- ✅ -->
<a href="/tafsir/al-fatiha">الفاتحة</a>                <!-- ❌ سينكسر -->
```

---

## ٦. المراسي (anchors)

- الصيغة المعتمدة: `part-1` / `part-2` / `part-3` و `cat-*`.
- الروابط القديمة (`#part1`, `#sec-meanings`, `#meanings`) **يجب أن تظل تعمل**.
- تُولَّد تلقائيًا عبر `legacyAliasesFor()` في `src/utils/anchors.ts`.
- **لا تحذف مرسًى قديمًا** بحجة التنظيف — قد يكون منشورًا في مكان ما.

---

## ٧. إضافة صفحة تفسير جديدة

اتبع `NEW_PAGE_GUIDE.md` خطوة بخطوة. باختصار: أنشئ مجلدًا في `src/content/tafsir/`، وضع فيه `meta.json` والملفات الثلاثة. **لا تسجيل يدوي مطلوب** — الاكتشاف تلقائي.

---

## ٨. الأوامر بعد أي تعديل

```bash
npm run build                                   # يجب أن ينجح
npm run check                                   # فحص الأنواع
npm run verify:content -- ../_sources/AL-fataha/index.html al-fatiha
```

الثلاثة يجب أن تنجح قبل اعتبار العمل منتهيًا.

---

## ٩. Git

- أنشئ فرعًا جديدًا قبل أي تعديل.
- لا تحذف المستودعات القديمة في `_sources/` ولا تعدّلها — هي مرجع للقراءة فقط.
- رسائل commit واضحة بالإنجليزية أو العربية.
